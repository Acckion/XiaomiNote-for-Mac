# 未来工作

## 菜单命令链路（spec-121 后续）

- 剩余业务操作的 Command 化（格式菜单弹窗、视图选项、音频面板、离线操作等）
- 菜单验证逻辑与 Command 的 canExecute 整合
- 拆分 `MenuActionHandler`（当前 1316 行）为独立职责文件
- 删除 `MenuActionHandler` 内同一动作的 fallback 双路径（保留单一命令入口）
